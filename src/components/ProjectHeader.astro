---
import SectionHeader from './SectionHeader.astro';
import { Suitcase } from './icons/Suitcase';

interface Props {
  forHomePage?: boolean;
  class?: string;
}

const projectMetaData = {
  title: "What I've been building",
  description: "Apps, platforms, and research that I've shipped."
};
---

<SectionHeader
  section="Projects"
  icon={Suitcase}
  {...projectMetaData}
  {...Astro.props}
/> 